.venv/
__pycache__/
*.pyc
.env
.env.*
*.key
*.pem
credentials*.json
.DS_Store
.claude/settings.local.json
spikes/out/

# runtime job state; the committed demo under data/demo/ is the exception
data/*
!data/demo/
!data/demo/**
# the manual cache baked into the container image: the JSON excerpts are source and
# travel with the repo, the downloaded PDFs (11 MB each) never do
!data/manuals/
data/manuals/*.pdf
data/manuals/_tmp/
